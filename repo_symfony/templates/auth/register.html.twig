{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
<div class="container">
    <div class="w-full max-w-md m-auto p-6 bg-white rounded-lg shadow-md">
        <div class="text-center mb-6">
            <h1 class="text-3xl font-bold text-gray-800">Inscription</h1>
            <p class="text-gray-500 mt-2">Créez votre compte pour accéder au système de réservation.</p>
        </div>

        {{ form_start(registrationForm) }}


            <div class="mb-2">
                {{ form_row(registrationForm.email, {'attr': {'class': 'form-input w-full py-3 px-4 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
            </div>

            <div class="mb-2">
                {{ form_row(registrationForm.plainPassword, {'attr': {'class': 'form-input w-full py-3 px-4 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
            </div>

            <div class="mb-2">
                {{ form_row(registrationForm.roles, {'attr': {'class': 'form-input w-full py-3 px-4 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
            </div>

            <div class="mb-2">
                {{ form_row(registrationForm.promotion, {'attr': {'class': 'form-input w-full py-3 px-4 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
            </div>

            <div class="mb-2">
                {{ form_row(registrationForm.agreeTerms, {'attr': {'class': 'form-checkbox text-blue-500'}}) }}
            </div>

            <button 
                type="submit" 
                class="btn btn-success w-full mt-4 font-extrabold  rounded-lg shadow-lg hover:bg-blue-600 transition duration-300 ease-in-out">
                S'inscrire
            </button>

        {{ form_end(registrationForm) }}
    </div>
</div>
{% endblock %}